# Cyclone local development configuration.
# Copy to .env. Never commit real values.

# Use forward slashes on Windows to avoid escaping ambiguity in Compose.
CYCLONE_VAULT_HOST_PATH=C:/Users/Agent/Documents/CycloneVault
CYCLONE_WORKSPACE_HOST_PATH=C:/Users/Agent/Documents/CycloneWorkspace

# Change every value below before use. Generate with a secure password manager
# or: openssl rand -hex 32
POSTGRES_DB=cyclone
POSTGRES_USER=cyclone
POSTGRES_PASSWORD=change-me-postgres-password
N8N_POSTGRES_DB=n8n
N8N_POSTGRES_USER=cyclone
N8N_POSTGRES_PASSWORD=change-me-n8n-password
N8N_ENCRYPTION_KEY=change-me-n8n-encryption-key-at-least-32-characters
N8N_USER_MANAGEMENT_JWT_SECRET=change-me-n8n-jwt-secret-at-least-32-characters

# Core adapter authentication. This must match the internal Hermes API server.
HERMES_API_KEY=change-me-hermes-api-key-at-least-32-characters
CYCLONE_INTERNAL_API_KEY=change-me-internal-event-key-at-least-32-characters
CYCLONE_HOST_BRIDGE_TOKEN=change-me-host-bridge-token-at-least-32-characters

# n8n uses this private service URL inside Docker to report events to Core.
CYCLONE_CORE_INTERNAL_URL=http://cyclone-core:8787

# Optional: set a local IANA timezone used by n8n schedules.
GENERIC_TIMEZONE=Europe/Amsterdam
TZ=Europe/Amsterdam

# Configure a provider inside persistent Hermes data, not by committing it here.
# For example, add DEEPSEEK_API_KEY only to your untracked local .env if you
# deliberately want Compose to pass it to Hermes:
# DEEPSEEK_API_KEY=
# OPENROUTER_API_KEY=

# Telegram stays disabled until explicitly configured locally.
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_ALLOWED_USERS=
# TELEGRAM_HOME_CHANNEL=
