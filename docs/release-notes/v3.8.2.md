# Cyclone 3.8.2

Cyclone 3.8.2 is a coordinated Android and Windows identity bump on the `grok/cyclone-3.8.2-fixes` line. It does **not** merge to `main` and does **not** overwrite tag or release `v3.8.1`.

Built from `grok/cyclone-3.8.2-fixes` after PRs 45/46/47/44/49.

## Identity

- Product version `3.8.2`
- Android `versionCode` 47 (`versionName` 3.8.2)
- Teamwork Sniper remains `3.5.3.3` / `versionCode` 6
- `publication_authorized = false` (do not publish yet)

## Fixes in this candidate

- **T0** — Canonical MCP action envelope: successful Android after-state is no longer reclassified as `PROTOCOL_MISMATCH`.
- **T1** — Compact observations keep non-empty `pageText` and `pageSummary` strings.
- **T2** — Authorized `phone.type` targets an observation-scoped editable element.
- Overlay task copy IDLE → DONE is restored after task entry works.
- **GATE** — Pay, send, delete, and permission-grant remain phone-side and cannot be bypassed.

## Verification

Physical Pixel 8 remains **UNVERIFIED** until T0–T4 pass on device. Do not merge to `main`.
