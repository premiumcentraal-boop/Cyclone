# 4.3.6 stability lane — not a release

Base: published v4.3.5, 55b64770 (4.3.5 / 96). No unrelated branch merged.

## History audit

The published tag contains all 13 commits after v4.3.4, including lifecycle (884f5378), observation isolation (22308799), recovery (417d808c), cookie interruption (f89c4456), compound login evidence (49ce401d), visual grounding (37d7802f), diagnostics (f7a8c1dc), task-window binding, consent context and shared screenshot-budget follow-ups, ending at 9a349889. The cut 55b64770 only changes release identity/docs/test label. Thus the release was not limited to two checkpoints. This does not prove device reliability.

## Checkpoint: secure API-key editor

Confirmed source defect: active ModelApi426Card and the older Model & API route called throwing save/clear methods directly from Compose click handlers. Invalid input, Android Keystore errors or preference commit failures could terminate the Activity/process. No phone crash trace is available, so the specific exception reported by the owner remains unconfirmed.

Both routes now share one editor: validates input; performs encryption/storage off the UI thread; catches storage failures into bounded secret-free copy; only acknowledges save after exact decrypted readback; keeps retry possible; handles removal failure; never uses rememberSaveable for the plaintext draft. Existing bootstrap save semantics and encryption remain intact.

Tests cover invalid input, storage exceptions, secret-free failure text, readback mismatch, successful save and failed removal.

Local Gradle cannot download 8.9 (network unreachable). GitHub CI required. No device testing performed.

## Remaining acceptance

- Compile, unit tests, lint through CI.
- Real phone: add/remove key, malformed key, locked/unavailable Keystore; obtain sanitized crash trace if failure persists.
- Reddit: overlay-visible reject optional cookies -> continue original login goal -> exact human handoff -> fresh resume; no false authenticated completion.
- Broader screenshot-only actuation is not implemented by the 4.3.5 semantic visual-grounding fallback; do not claim it is.
- Do not mark 4.3.6 stable or publish until remaining failures are evaluated. This checkpoint retains 4.3.5 metadata deliberately.

## Further audited gaps

- Login evidence now respects Android's visibleToUser field, preventing hidden sign-out controls from proving authentication. Regression added.
- Visual grounding now rejects missing session/observation identity and negative displays explicitly; two missing session strings previously compared equal. Existing PhoneToolExecutor grounding remains authoritative. Non-mutating human handoff is still retained on an expired image.
- Enabled Mobile CI on this dedicated branch; no publication workflow is triggered.
- Local repository guards: 72 tests passed; mobile product guard passed. Android suite/lint/assembly pending CI at this checkpoint.

Published checkpoints: c7966213307b7114ec0081f34723ceef75f182db (API editor); e8011650163369f246c86df2fd18cf15220e190d (login evidence + CI).

## Validation result — 11 September 2026

Code checkpoint 2240df7f9114698276b5704b999c1b1d892c78e5 passed Mobile CI:
https://github.com/premiumcentraal-boop/Cyclone/actions/runs/34651760542
Job 103435355017: success. Android unit tests, lint and unsigned release assembly succeeded (`BUILD SUCCESSFUL in 7m 22s`). Repository/gateway/MCP guards in the same workflow passed. Local 72 repository tests also passed. This supersedes the earlier pending-CI notes above.

All three code checkpoints are pushed. This final documentation-only checkpoint does not change the tested source tree's product code.

Release decision: source repairs are ready for review and device acceptance, not an assertion of a stable 4.3.6 release. No tag, signing change or publication was performed. Version remains 4.3.5 / 96 until an approved version cut; do not distribute the CI artifact as 4.3.6. Physical API-key entry and Reddit login remain UNVERIFIED. Obtain a sanitized Android crash trace if key entry still fails; the original reported exception itself was not available.
