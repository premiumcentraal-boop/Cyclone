name: Legacy Cyclone 2.9.5 Build (manual compatibility)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  compatibility-build:
    uses: ./.github/workflows/_mobile-build.yml
    permissions:
      contents: read
