name: Legacy Cyclone 2.9.4 Build (manual compatibility)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  compatibility-build:
    uses: ./.github/workflows/_mobile-build.yml
    permissions:
      contents: read
