{
  "name": "@cyclone/one",
  "private": true,
  "version": "0.1.0",
  "description": "Cyclone One desktop phone gateway",
  "type": "module",
  "scripts": {
    "dev": "vite --port 1420 --strictPort",
    "build": "tsc -p tsconfig.json && vite build",
    "tauri": "tauri",
    "test": "tsc -p tsconfig.test.json && node --test tests/*.test.mjs",
    "test:ui": "npm run test"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "qrcode": "1.5.4"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.5",
    "@types/qrcode": "1.5.5",
    "typescript": "^5.8.3",
    "vite": "^7.1.3"
  }
}
