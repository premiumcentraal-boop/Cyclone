{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal desktop capability set for the Cyclone PC Companion UI shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
