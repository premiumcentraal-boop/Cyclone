import test from"node:test";import assert from"node:assert/strict";import{canPreserveFocusedPage,initialCompanionState,reduceCompanionState}from"../.test-dist/core/fleet.js";import{createMockDevices}from"../.test-dist/services/mockDesktopService.js";
test("fleet add/remove keeps focus only while selected phone exists",()=>{const devices=createMockDevices(2);let s=initialCompanionState(devices);s=reduceCompanionState(s,{type:"focus_device",deviceId:devices[0].id});assert.equal(s.route,"focused");s=reduceCompanionState(s,{type:"devices_updated",devices:[...devices,{...devices[0],id:"phone-3"}]});assert.equal(s.route,"focused");s=reduceCompanionState(s,{type:"devices_updated",devices:[devices[1]]});assert.equal(s.route,"fleet");assert.equal(s.focusedDeviceId,null)});
test("focused phone navigation has explicit back to fleet",()=>{const devices=createMockDevices(1);let s=reduceCompanionState(initialCompanionState(devices),{type:"focus_device",deviceId:devices[0].id});s=reduceCompanionState(s,{type:"back_to_fleet"});assert.equal(s.route,"fleet");assert.equal(s.focusedDeviceId,null)});
test("fleet refresh preserves a mounted focused stream while its device still exists",()=>{const devices=createMockDevices(1);const s=reduceCompanionState(initialCompanionState(devices),{type:"focus_device",deviceId:devices[0].id});assert.equal(canPreserveFocusedPage(s,[{...devices[0],connectionLabel:"Heartbeat refreshed"}]),true);assert.equal(canPreserveFocusedPage(s,[]),false)});
