[package]
name = "cyclone-desktop"
version = "0.1.0"
description = "Cyclone agent operating environment"
authors = ["Northstar Labs"]
edition = "2021"
rust-version = "1.77"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[features]
custom-protocol = ["tauri/custom-protocol"]
