# Cyclone Claude entry point

Read and obey root `AGENTS.md` before editing Cyclone.

Current product is **3.6.0**. Work from `release/beta/cyclone-3.6.0` unless the task is to fast-forward `main` (see `docs/WORKING_LINE.md`). `main` may still be 3.5.1.

Canonical knowledge: `docs/agent-system/README.md`. Run `python scripts/agent/cyclone-context.py --markdown`.

Do not treat `docs/STATUS.md`, `docs/HANDOFF.md`, `docs/ARCHITECTURE.md`, or `CYCLONE_V2_*` as current authority. Those are archive stubs.
