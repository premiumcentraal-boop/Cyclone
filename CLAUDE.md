# Cyclone Claude entry point

Read and obey root `AGENTS.md` before editing Cyclone. Use `docs/agent-system/README.md` as the canonical project knowledge hub and run `python scripts/agent/cyclone-context.py --markdown` to inspect the current checkout.

Do not treat historical version handoffs as current authority when they conflict with code/tests or the canonical agent-system docs.
