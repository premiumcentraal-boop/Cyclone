# Cyclone Gemini entry point

Read root `AGENTS.md` first. The canonical project knowledge is under `docs/agent-system/`; run `python scripts/agent/cyclone-context.py --markdown` before changing code.

Keep the Android action/learning/gateway ownership boundaries and product invariants defined in `AGENTS.md`.
