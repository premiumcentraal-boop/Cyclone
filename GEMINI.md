# Cyclone Gemini entry point

Read root `AGENTS.md` first. Current product is **3.6.0** on `release/beta/cyclone-3.6.0`.

Canonical knowledge is under `docs/agent-system/`. Run `python scripts/agent/cyclone-context.py --markdown` before changing code.

Keep the Android action / learning / gateway ownership boundaries and product invariants in `AGENTS.md`. Ignore archived root handoffs.
