# Teamwork Sniper Accessibility Probe

Diagnostic-only tooling for parsing Teamwork `uiautomator dump` XML into normalized shift candidates. This tool must remain semantic-only: no screenshots, OCR, vision, or pixel parsing.

Live XML captures belong in `fixtures/` only when collected from the target device. Synthetic fixtures must use the `synthetic_` prefix.
